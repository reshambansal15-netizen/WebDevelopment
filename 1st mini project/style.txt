body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 8px;
    border-radius: 10px;
    font-size: 13px;
    background-color: #e87706;
}

/* Header */
header {
    text-align: center;
    margin-bottom: 10px;
}

header h1 {
    margin: 0;
    font-size: 35px;
}

header p {
    color: gray;
    margin: 2px;
    font-size: 12px;
}

/* Cards Section */
.cards {
    display: flex;
    gap: 8px;
    margin-bottom: 10px;
}

.card {
    background: rgb(15, 234, 29);
    padding: 10px;
    flex: 1;
    border: 1px solid #ddd;
    border-radius: 8px;
    text-align: center;
}

/* Categories */
.categories {
    background: rgb(15, 207, 233);
    padding: 10px;
    border-radius: 8px;
    margin-bottom: 10px;
}

.category {
    display: flex;
    justify-content: space-between;
    padding: 4px 0;
    border-bottom: 1px solid #eee;
}

/* Transactions */
.transactions {
    background: rgb(135, 9, 102);
    padding: 6px;
    border-radius: 5px;
}

.transaction {
    border: 1px solid #ddd;
    padding: 2px;
    border-radius: 4px;
    margin-bottom: 3px;
}

.transaction span {
    color: gray;
    display: block;
    margin-top: 5px;
}

/* 🔥 MOBILE RESPONSIVE */
@media (max-width: 768px) {

    body {
        padding: 10px;
    }

    /* Stack cards vertically */
    .cards {
        flex-direction: column;
    }

    .card {
        width: 100%;
    }

    /* Adjust text size */
    header h1 {
        font-size: 22px;
    }

    header p {
        font-size: 14px;
    }

    /* Category spacing */
    .category {
        font-size: 14px;
    }

    /* Transactions spacing */
    .transaction {
        padding: 12px;
    }
}